$(function() {

    var header = document.getElementById('header');
    var headroom = new Headroom(header);
    headroom.init();

    //menu responsive
    //calculamos el ancho de la pagina
    var ancho = $(window).width().
        enlaces = $('#enlaces-t').
        btnmenu = $('#btn-menu').
        icono = $('#btn-menu .icono-1');

        if(ancho < 700){
            enlaces.hide();
        }
});